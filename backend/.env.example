# API server port
PORT=3001

# Path to the SQLite database file (relative to backend/ dir or absolute)
DB_PATH=../data/coffee.db

# Allowed CORS origin (your frontend URL)
CORS_ORIGIN=http://localhost:5173
